/**
 * Use to assign type in field data from tables
 */
export enum ITableFieldType {
    BUTTON = 'button',
    TEXT = 'text',
    TEXT_ACTION = 'text_action',
    DATE = 'date',
    LINK = 'link',
    VALUE = 'value',
    CHECKBOX = 'checkbox',
    CHECKBOX_INSIDE = 'checkboxInside',
    ICON = 'icon',
    NUMBER = 'number',
    PERCENTAGE = 'percentage',
    TITLE = 'title',
    SELECT = 'select',
    RATING = 'rating',
    SELECT_SEARCH = 'selectSearch',
    TEXT_AREA = 'TEXT_AREA',
    OBJECT = 'object',
}

/**
 * Default ID by table component
 */
export const ID_CONTENT_TABLE = 'containerTableComponent';
