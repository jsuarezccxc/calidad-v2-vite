/**
 * This constant is used to update the created products in massive upload
 */
export const UPDATE = 'UPDATE';

/**
 * This constant is used to skip the products loaded in massive upload
 */
export const SKIP = 'SKIP';

/**
 * Option that validates if is product
 */
export const PRODUCT = 'PRODUCT';

/**
 * Option that confirms the variants
 */
export const WITH_VARIANTS = 'WITH_VARIANTS';

/**
 * Const that indicates the save in local storage
 */
export const HOW_TO_ADD_PRODUCTS = 'HOW_TO_ADD_PRODUCTS';

/**
 * Option that validates  the upload is one by one
 */
export const ONE_BY_ONE = 'ONE_BY_ONE';

/**
 * This constant indicates the number the products for show table in modal delete products
 */
export const ONE_PRODUCT = 1;

/**
 * Query param used to indicate that this page belongs to a new version
 */
export const NEW_VERSION = 'new-version';

/**
 * Query param used to indicate that add new product
 */
export const ADD_PRODUCT = 'add-product';

/**
 * Query param used to indicate that add new product section add product
 */
export const ADD_TAXES = 'add-taxes';

/**
 * This constant represent the milliliter to calculate tax ibua value
 */
export const MILLILITER_TEXT = 'Mililitros';
