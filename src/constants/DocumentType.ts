export const PA = 'PA';

/**
 * Document type query parameter values for URL routing
 */
export const DOCUMENT_TYPE_PARAMS = {
    QUOTE: 'quote',
} as const;

// Pendiente agregar las demas constantes de tipo de documento
