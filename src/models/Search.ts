/**
 * This interface describes the properties of the nested key
 * 
 * @typeParam prop: string - Property of the nested object used in the filter
 * @typeParam key: string - Key of the nested object used in the filter
 */
export interface INestedKey {
    prop: string;
    key: string;
}